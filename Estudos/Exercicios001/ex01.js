function operacoes(x, y) {
    soma = x + y
    subtracao = x - y
    multiplicacao = x * y
    divisao = x / y

    return console.log(`Soma = ${soma}, Subtração = ${subtracao}, Multiplicação = ${multiplicacao}, Divisao = ${divisao}`)
}


operacoes(2, 5)
operacoes(3, 10)